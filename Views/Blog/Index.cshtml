@model List<PolatPortfolio.Models.BlogPost>
@{ ViewData["Title"] = "Blog"; }

<h2>Blog Yazıları</h2>
@foreach(var post in Model) {
    <div class="card mb-3 bg-secondary text-white">
        <div class="card-body">
            <h3>@post.Title</h3>
            <p>@post.Content</p>
            <small>@post.Date.ToShortDateString()</small>
            <p><a href="/Blog/Details/@post.Id" class="btn btn-link">Devamını Oku</a></p>
        </div>
    </div>
}
